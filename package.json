{
  "name": "home.tab.bookmark.docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "rm -rf ./public && mkdirp ./public && rm -rf ./private && mkdirp ./private",
    "devbuild": "npm run --silent prepare && cp -R ./src/* ./public/ && rm ./public/script.js && cp ./src/script.js ./private/",
    "devtest": "npm run --silent devbuild && nodemon index.js",
    "build": "npm run --silent prepare && cp -R ./src/* ./public && rm ./public/script.js && uglifycss ./src/style.css --output ./public/style.css && uglifyjs ./src/script.js --compress --mangle --warn --output ./private/script.js",
    "buildtest": "npm run --silent build && node index.js",
    "dockerbuild": "rm -rf ./home-tab-bookmark.tar && npm run --silent build && docker build -t dannystudio/home-tab-bookmark .",
    "dockertest": "docker run -p 3000:3000 dannystudio/home-tab-bookmark",
    "dockersave": "npm run dockerbuild && echo build completed, now save to tar... &&docker save -o ./home-tab-bookmark.tar dannystudio/home-tab-bookmark"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "express-formidable": "^1.2.0",
    "mkdirp": "^3.0.1",
    "sharp": "^0.33.5",
    "uglify-js": "^3.19.3",
    "uglifycss": "^0.0.29"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "puppeteer": "^24.2.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
